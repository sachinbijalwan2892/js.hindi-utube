//WINDOW 

//window is a golbal object means it has no scope u can access it anywhere u want 
//it is created by browser and it represents browser window
//dom, bom, and javascript core functionalities lie under it 
//it is a top level global entity






//DOM
//dom is document object model 
//if we convert our html code into js object than it becomes document and this as a whole
//is known as document object model








//BOM

//other than manipulation of web page or elements editing if js wants to communicate with browser 
//then bom helps in this 
//browser object model